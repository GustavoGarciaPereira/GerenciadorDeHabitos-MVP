/**
 * Placeholder for the progress grid (Mission 2+).
 * Renders an empty slot that will later show the 7-day/week grid + streak.
 */
export default function HabitGrid(props) {
  return (
    <div class="habit-grid-placeholder">
      <p class="habit-grid-placeholder__text">
        Grade de progresso será implementada na próxima missão.
      </p>
    </div>
  );
}
